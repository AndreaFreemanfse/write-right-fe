// Global setup: injects localStorage onboarding flag into all test contexts
async function globalSetup(config) {
  // Nothing needed here - we use per-project init scripts in playwright config
}

module.exports = globalSetup;
